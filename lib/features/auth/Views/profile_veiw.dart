import 'package:flutter/material.dart';

class ProfileVeiw extends StatelessWidget {
  const ProfileVeiw({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Profile View')));
  }
}
